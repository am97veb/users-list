services:
  db:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: demoPassword
      POSTGRES_DB: users_list
    ports: 
      - "5432:5432"
    volumes: 
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: